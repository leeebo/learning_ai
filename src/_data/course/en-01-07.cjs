module.exports = [
  {
    n: 1,
    t: 'Neural Network Fundamentals',
    s: 'Build intuition from tensors, layers, and loss functions',
    goal: 'Understand how a neural network turns input tensors into optimizable predictions, and learn to follow one forward and backward pass.',
    concept: [
      'Tensor shapes, weights, and biases',
      'Activation functions and nonlinearity',
      'Loss functions, gradients, and backpropagation',
      'Rough estimates of parameter counts and MACs',
    ],
    analogy: 'Think of a network as a data pipeline: tensors are USB buffers, layers are protocol-conversion modules, weights are firmware constants, and gradients resemble error feedback collected in the field.',
    diagram: 'Input x\n  │ Linear(Wx+b)\n  ▼\nActivation f(·) ──► Prediction ŷ ──► Loss(ŷ,y)\n  ▲                                      │\n  └──────── Gradient backprop ◄──────────┘',
    code: 'y = x @ W.T + b\ny = relu(y)\nloss = mean((y - target) ** 2)\nloss.backward()',
    lab: 'Using a 2→3→1 MLP, calculate every layer shape, the parameter count, and one MSE by hand; then verify the gradient direction with any framework.',
    questions: [
      'Why are multiple linear layers still equivalent to one linear layer when there is no activation function between them?',
      'For an input shape of [1,3,32,32], how do you calculate the output shape of the first convolution?',
      'Which resource costs do parameter count and MACs approximate, respectively?',
    ],
    next: 'Training and Inference',
    lesson: [
      {
        title: 'Read strides and axis semantics, not just shape',
        body: 'The same 3,072 numbers mean entirely different things in [1,3,32,32] and [1,32,32,3]; even with the same shape, a transposed view may be non-contiguous. Layers normalize, convolve, or reduce along agreed axes, while broadcasting can make a wrong shape “run successfully.” When reading a network, label every axis with N/C/H/W, units, and dtype, and print strides at boundaries so that executability is never mistaken for semantic correctness.',
      },
      {
        title: 'Nonlinearity changes the decision boundaries that can be composed',
        body: 'An affine layer only rotates, scales, and translates; multiple affine maps can still be folded into one matrix multiplication. Activations such as ReLU divide the input space into regions governed by different linear relationships, so stacking layers genuinely increases expressive power. In engineering work, also inspect activation ranges: large dead regions may signal shifted inputs or poor initialization, while excessively large values may saturate during low-precision deployment.',
      },
      {
        title: 'The loss is the objective interface; gradients are the layer-by-layer credit ledger',
        body: 'A loss function specifies which discrepancies deserve a penalty; the chain rule then propagates the sensitivity of total error to each intermediate value, one layer at a time. A zero gradient does not necessarily mean learning is complete—it may come from saturated activations, a detached computation graph, or numerical underflow. An exploding gradient can likewise make one update overshoot the useful region. Training experiments should record loss, gradient norms, and parameter-update magnitudes rather than watching final accuracy alone.',
      },
      {
        title: 'Parameters, MACs, and peak activations answer three different questions',
        body: 'Parameter count roughly estimates weight storage; MAC count roughly estimates multiply–accumulate work; activation lifetimes determine how large the runtime workbench must be. A convolution may have few weights yet produce a huge feature map, while an elementwise operator has almost no parameters but still reads and writes an entire tensor. For an edge-device budget, draw live intervals in execution order and add workspace, alignment padding, and I/O buffers to the peak.',
      },
      {
        title: 'Use finite differences and intermediate tensors to verify that the math is right',
        body: 'For a tiny network, freeze weights and inputs and save each layer output, the loss, and analytical gradients. Then perturb one parameter by a small positive and negative ε and approximate its derivative from the two loss values. A mismatch often comes from a transpose, batch reduction, in-place mutation, or an activation boundary. Deployment does not need backpropagation, but these golden tensors remain valuable for checking that export, quantization, and kernels preserve forward semantics.',
      },
    ],
    pitfall: 'Comparing parameter counts alone ignores activation tensors and workspace. On an MCU, a crash is often caused by peak RAM at the instant of inference, not by the size of the weight file in Flash.',
    references: [
      ['PyTorch ONNX Export', 'https://docs.pytorch.org/tutorials/beginner/onnx/export_simple_model_to_onnx_tutorial.html'],
      ['LiteRT for Microcontrollers', 'https://ai.google.dev/edge/litert/microcontrollers/get_started'],
    ],
    quiz: [
      {
        prompt: 'Why can two linear layers be merged when there is no activation between them?',
        options: ['Every layer normalizes automatically', 'The composition of linear mappings is still a linear mapping', 'Weights disappear during inference', 'The MAC count must be zero'],
        answer: 1,
        explanation: 'Without a nonlinearity, W₂(W₁x+b₁)+b₂ can be rearranged into a new W and b; adding depth has not added any nonlinear decision boundary.',
      },
      {
        prompt: 'For an input shape of [1, 3, 32, 32], what should you confirm first?',
        options: ['Only the total byte count', 'Whether channel position and dtype match the model contract', 'Change the batch to 32 first', 'Upload it to the NPU first'],
        answer: 1,
        explanation: 'Axis semantics and dtype are part of the model I/O contract. An NCHW/NHWC or int8/float mismatch may run while producing consistently wrong results.',
      },
      {
        prompt: 'Which rough metric is more directly related to edge inference latency?',
        options: ['Length of the model filename', 'Parameter count', 'MACs together with data movement', 'Number of training epochs'],
        answer: 2,
        explanation: 'Parameter count mainly affects weight storage. MACs describe computation, but activation traffic, caches, and bandwidth must also be considered to explain real latency.',
      },
    ],
    readingMinutes: 18,
    keywords: [
      {term: 'Tensor', definition: 'A multidimensional numeric block with a shape and data type—the common representation for model inputs, weights, and intermediate results.', espAnalogy: 'Like a DMA buffer annotated with sample count, channel count, and bit width.'},
      {term: 'Shape', definition: 'The length and meaning of each dimension, such as batch, channel, height, and width in NCHW.', espAnalogy: 'Like the field layout of a protocol frame: the bytes remain when the order is wrong, but their meaning is corrupted.'},
      {term: 'Activation function', definition: 'A nonlinearity applied after a linear transform that lets multilayer networks express complex boundaries.', espAnalogy: 'Like a conditional branch in a state machine that cannot simply be folded away.'},
      {term: 'MAC', definition: 'One multiply–accumulate operation, commonly used for a rough estimate of compute.', espAnalogy: 'Like the number of iterations in an inner DSP loop, still subject to the cost of fetching data.'},
    ],
    recap: 'This is the starting point of the course. You already know the ESP32 path in which a peripheral produces data, DMA moves it, and a task consumes it. This chapter abstracts that path into neural-network inputs, layers, and outputs, establishing a shared language for the training and deployment discussions ahead.',
    nextPreview: 'The next chapter separates today’s forward computation into training-time and inference-time concerns: which state belongs in the lab, and which state can be flashed to a target as stable firmware.',
    history: {
      intro: 'Neural networks did not suddenly appear in the age of “large models.” They followed a long path from logical neurons and trainable linear classifiers, through credit assignment in multilayer networks, to large-scale representation learning on GPUs. The tensors, operators, automatic differentiation, and deployment graphs used in edge engineering today are layered interfaces accumulated along that path.',
      milestones: [
        {
          year: '1943',
          title: 'The neuron was first written as a computable logical unit',
          body: 'McCulloch and Pitts described neural activity with threshold units and connection networks, showing that simple units could be composed into logical behavior. Their work had no modern training algorithm, but it established the computational view that complex functions can emerge from many uniform, connected units.',
          source: {label: 'Original paper by McCulloch and Pitts', url: 'https://www.cs.cmu.edu/~epxing/Class/10715/reading/McCulloch.and.Pitts.pdf'},
        },
        {
          year: '1958',
          title: 'The perceptron turned “connections” into weights corrected by examples',
          body: 'Rosenblatt’s perceptron introduced a mechanism for adjusting connection weights from classification errors, moving neural networks from hand-crafted logical structures toward data-driven learning. It mainly addressed linearly separable problems, but made “model parameters are the result of training” a central idea for later networks.',
          source: {label: 'Original perceptron paper by Rosenblatt', url: 'https://doi.org/10.1037/h0042519'},
        },
        {
          year: '1986',
          title: 'Backpropagation gave hidden layers computable error signals',
          body: 'Rumelhart, Hinton, and Williams demonstrated how error backpropagation calculates derivatives layer by layer and adjusts weights. Hidden units no longer needed hand-assigned meanings; they could form internal representations for a task, giving multilayer nonlinear networks a practical, unified training method.',
          source: {label: 'Original Nature backpropagation paper', url: 'https://www.nature.com/articles/323533a0'},
        },
        {
          year: '2012',
          title: 'AlexNet carried deep representation learning into the big-data and GPU era',
          body: 'AlexNet trained a deep convolutional network on a large image dataset and used GPUs for dense tensor computation. The breakthrough came not only from its architecture, but from data, parallel computing, and regularization arriving together. Parameter count, MACs, memory, and throughput consequently became parts of one engineering budget.',
          source: {label: 'Original AlexNet paper', url: 'https://papers.nips.cc/paper/4824-imagenet-classification-with-deep-convolutional-neural-networks'},
        },
        {
          year: '2019',
          title: 'Imperative tensor programs and automatic differentiation became everyday tools',
          body: 'The PyTorch paper summarized a combination of an imperative front end, dynamic graphs, and a high-performance tensor backend. Researchers could write control flow like ordinary programs while the system recorded operations and calculated gradients. Today’s need to confirm dynamic paths before export follows directly from that flexibility.',
          source: {label: 'Original PyTorch systems paper', url: 'https://papers.neurips.cc/paper/2019/hash/bdbca288fee7f92f2bfa9f7012727740-Abstract.html'},
        },
      ],
      bridge: 'History leaves a practical lesson: network capability comes from composable nonlinear units, learning comes from a traceable error chain, and performance comes from mapping tensor programs effectively onto hardware. When reading a model for edge deployment, follow three threads at once: how values transform, how gradients once shaped the weights, and how buffers remain live during deployment.',
    },
    visual: {
      title: 'One training pulse: how data becomes a weight update',
      description: 'Each step in the player illuminates a tensor and its owner. Signals flow to the right in the first half, gradients travel back to the left in the second, and the weight scale moves slightly at the end.',
      steps: [
        {icon: '📥', label: 'Load samples', data: 'x:[B,2], y:[B,1]', action: 'Highlight the batch axis and send the input into the first layer', insight: 'Shape, dtype, and axis semantics must be fixed before later computations can share a contract'},
        {icon: '🧮', label: 'Affine transform', data: 'z = xWᵀ + b', action: 'Connections flow into each output unit with thickness proportional to weight', insight: 'W controls the mixing direction and b shifts the boundary; this step is still linear'},
        {icon: '⚡', label: 'Apply activation', data: 'a = ReLU(z)', action: 'Negative-valued nodes go dark while positive nodes keep their brightness', insight: 'Nonlinearity prevents a stack of layers from collapsing into a single matrix'},
        {icon: '🎯', label: 'Calculate loss', data: 'L = mean((ŷ-y)²)', action: 'An error ruler appears between each prediction and target', insight: 'A scalar loss turns a task objective into a differentiable optimization interface'},
        {icon: '↩️', label: 'Backpropagate gradients', data: '∂L/∂W = ∂L/∂ŷ · …', action: 'Error signals travel backward over the recorded operation edges', insight: 'The chain rule assigns credit; each gradient shape must match its parameter'},
        {icon: '🔧', label: 'Update parameters', data: 'W ← W − η∇W', action: 'The control moves one small notch and the next prediction refreshes', insight: 'The learning rate sets the step size; a new forward pass must verify the update direction'},
      ],
      loop: 'The updated weights receive the next batch. The animation returns to “Load samples” while preserving a gradually descending loss trace as state across iterations.',
    },
    analogyDetail: {
      title: 'A bank of mixing consoles that tunes itself from sound-check notes',
      story: 'Imagine a band’s multitrack recording entering several rows of mixing consoles. Each row mixes channels according to knob positions, then passes them through noise gates that admit only certain signals. At the end, an engineer compares the mix with a reference recording and sends correction notes—“too loud here, too weak there”—backward along the signal path. After repeated sound checks, the knob positions are the learned weights.',
      illustration: [
        {icon: '🎚️', label: 'Rows of knobs', mapsTo: 'Weights and biases determine how much each input contributes to the next layer'},
        {icon: '🚪', label: 'Noise gates', mapsTo: 'Activation functions introduce nonlinearity and change the expressible boundaries'},
        {icon: '🎧', label: 'Reference monitor', mapsTo: 'The loss function condenses prediction-versus-target differences into an optimizable error'},
        {icon: '📝', label: 'Notes sent backward', mapsTo: 'The chain rule apportions output error to every parameter'},
      ],
      boundary: 'This analogy only helps explain signal transformation and error feedback. A real network “knob” participates in high-dimensional batched operations, and a gradient is a precise composition of local derivatives rather than a subjective opinion. The analogy also cannot represent parameter sharing, convolution layouts, or coupling introduced by numerical precision.',
    },
  },
  {
    n: 2,
    t: 'Training and Inference',
    s: 'Understand the boundary between offline training and on-device execution',
    goal: 'Distinguish training, inference, and deployment graphs, and know which state must be retained and which can be folded away.',
    concept: ['Epochs, batches, learning rates, and optimizers', 'Training mode and inference mode', 'BatchNorm folding and constant folding', 'Why edge devices execute only the forward pass'],
    analogy: 'Training is like debugging a protocol stack in the lab; inference is like flashing a finalized state machine onto a device. The device should not carry training gradients, optimizers, or temporary statistics.',
    diagram: 'Dataset → training loop → checkpoint → export/optimize → runtime → input → output',
    code: 'model.eval()\nwith no_grad():\n    output = model(input)\nexport(model, example_input, format="onnx")',
    lab: 'Compare BatchNorm outputs in training and inference modes. Fix the random seed before and after export, then verify numerical error and the operator list.',
    questions: ['Why is Dropout disabled during inference?', 'Which training state must not be carried directly onto an MCU?', 'Why does model export need an example input?'],
    next: 'Model Formats',
    lesson: [
      {title: 'A training loop advances both parameter state and optimizer state', body: 'A step is more than a forward pass followed by subtracting gradients. Optimizers such as Adam maintain historical statistics for every parameter, and learning-rate schedulers have their own step counts. Resuming from weights alone while omitting optimizer state and epoch changes the training trajectory; none of that state is useful for pure inference. Treat “resumable checkpoint” and “deployable weights” as two separately named, validated, and archived artifacts.'},
      {title: 'Verify mode switches module by module; do not trust one Boolean', body: 'Calling eval recursively switches the behavior of modules such as Dropout and BatchNorm, but a custom layer may still read a training flag or use random numbers. BatchNorm running means and variances may also be distorted by small batches or data drift. Before export, run the same input repeatedly to confirm deterministic output, then inspect random operators, statistics buffers, and requires-grad state individually.'},
      {title: 'A deployment graph is a transformed artifact, not a photocopy of the checkpoint', body: 'An exporter may inline functions, remove backward nodes, fold constants, merge Conv and BatchNorm parameters, or specialize control flow using an example input. These changes reduce scheduling and memory overhead, but may capture only the branch exercised by the example. If a model contains data-dependent loops, dynamic shapes, or custom operators, define the export strategy explicitly rather than assuming every source-program path was preserved.'},
      {title: 'Training, validation, and field inputs need three separate boundary specifications', body: 'The training set updates parameters, the validation set selects hyperparameters and checkpoints, and a test set or field replay estimates final generalization. Mixing them makes metrics optimistic. Device deployment adds distribution factors such as sensor range, color conversion, and sample timing. Preserve raw field samples and labeling rules so the exported model can repeat the same acceptance suite without starting any training code.'},
      {title: 'Use layered equivalence tests to locate where export error begins', body: 'First compare train and eval modes in the source framework to confirm that differences come from expected modules. Then use fixed inputs to compare the eval model with the exported runtime. Finally, return raw device buffers to the PC for replay. Set absolute and relative tolerances per layer and record the first node that exceeds them. This separates mode errors, graph-conversion errors, quantization error, and device preprocessing errors.'},
    ],
    pitfall: 'Comparing model files only on a PC, without comparing on-device preprocessing, can misdiagnose color-space, quantization, and resize mistakes as “reduced model accuracy.”',
    references: [
      ['PyTorch ONNX Export', 'https://docs.pytorch.org/tutorials/beginner/onnx/export_simple_model_to_onnx_tutorial.html'],
      ['ONNX Concepts', 'https://onnx.ai/onnx/intro/concepts.html'],
    ],
    quiz: [
      {prompt: 'Why should model.eval() be called before inference?', options: ['To release all weights', 'To make Dropout and BatchNorm use inference behavior', 'To quantize automatically to int8', 'To convert the model to GGUF'], answer: 1, explanation: 'eval() neither quantizes nor deletes parameters. It switches modules affected by training state, preventing random Dropout masking and training-batch statistics in BatchNorm.'},
      {prompt: 'Which item should normally not be included in an MCU inference artifact?', options: ['Weight tensors', 'Input/output metadata', 'Optimizer momentum and gradients', 'Operator execution order'], answer: 2, explanation: 'Optimizer state and gradients update parameters. Edge inference executes only the forward pass of a fixed model, so carrying them wastes scarce storage and memory.'},
      {prompt: 'What is the main purpose of an example input during model export?', options: ['Add another training sample', 'Help the exporter capture input shapes and the actual operator path', 'Replace a representative calibration set', 'Set the device CPU frequency'], answer: 1, explanation: 'An example input supports graph capture and shape inference. Quantization calibration still needs representative data spanning the real input range.'},
    ],
    readingMinutes: 17,
    keywords: [
      {term: 'Epoch', definition: 'One complete traversal of the training set.', espAnalogy: 'Like completing one pass through an entire suite of system-level test cases.'},
      {term: 'Batch', definition: 'A small group of samples used to calculate one gradient update.', espAnalogy: 'Like collecting several sensor windows for one DSP operation.'},
      {term: 'eval mode', definition: 'A mode that makes Dropout and BatchNorm use inference semantics.', espAnalogy: 'Like switching from a debug configuration to a production configuration.'},
      {term: 'Optimizer', definition: 'An algorithm—and its state—that updates parameters from gradients.', espAnalogy: 'Like an automatic tuning controller that should not remain in deployed firmware.'},
    ],
    recap: 'The previous chapter treated a network as a data pipeline with an input/output contract. This chapter asks who modifies the weights, when those modifications stop, and exactly which state must remain on the device.',
    nextPreview: 'The next chapter packages the fixed inference graph in formats such as ONNX, LiteRT, or GGUF, with particular attention to whether the format preserves the I/O contract established here.',
    history: {
      intro: 'The line between training and inference became clearer as networks evolved from experimental apparatus into reusable software. Early learning rules changed weights as samples arrived; multilayer training introduced complete gradient state; then modules such as Dropout and BatchNorm gave “training mode” and “inference mode” different semantics. Mobile runtimes ultimately distilled deployment artifacts into stable forward graphs.',
      milestones: [
        {year: '1958', title: 'The perceptron learning rule formed a minimal training loop', body: 'The perceptron changed connection weights in response to prediction errors, closing the loop among samples, predictions, errors, and updates. Although the model was simple, it already separated the moment when examples modify parameters from the moment when current parameters produce a decision.', source: {label: 'Original perceptron paper by Rosenblatt', url: 'https://doi.org/10.1037/h0042519'}},
        {year: '1986', title: 'Backpropagation expanded training-time state and computation', body: 'A multilayer network must retain forward intermediates and calculate each parameter gradient during the backward phase. Training therefore became a longer computation graph than inference, containing activations, gradients, and an update process in addition to weights. The reason to deploy only the resulting parameters became explicit.', source: {label: 'Original Nature backpropagation paper', url: 'https://www.nature.com/articles/323533a0'}},
        {year: '2014', title: 'Dropout clearly exposed two execution semantics for one module', body: 'During training, Dropout randomly removes units to reduce co-adaptation; at test time it uses a deterministic approximation of the full network. Engineers consequently had to switch modes explicitly, or identical inputs would receive unstable outputs from training-time randomness.', source: {label: 'Original Dropout paper', url: 'https://www.jmlr.org/papers/v15/srivastava14a.html'}},
        {year: '2015', title: 'BatchNorm added running statistics beyond learned parameters', body: 'Batch Normalization computes statistics over training batches, then uses accumulated statistics together with learned scale and offset during inference. A checkpoint thus contains not only weight matrices but also buffers that change inference results, making a fixed mode before export essential.', source: {label: 'Original Batch Normalization paper', url: 'https://proceedings.mlr.press/v37/ioffe15.html'}},
        {year: '2017', title: 'Lightweight runtimes narrowed the device role to low-latency forward execution', body: 'The TensorFlow Lite developer preview targeted mobile and embedded devices, emphasizing a small footprint, fast initialization, and hardware acceleration. Converting models from a training framework into a constrained deployment format gave the toolchain explicit responsibility for separating training state and debugging convenience from device runtime constraints.', source: {label: 'Official TensorFlow Lite announcement', url: 'https://developers.googleblog.com/en/announcing-tensorflow-lite/'}},
      ],
      bridge: 'Reliable deployment today is not merely calling eval and saving a file. It creates a traceable relationship among training checkpoints, evaluated models, exported graphs, optimized graphs, and device binaries. Each artifact has its own state and version; whenever a step folds or replaces nodes, fixed inputs must prove that it still represents the same task function.',
    },
    visual: {
      title: 'How a checkpoint sheds weight to become a device forward graph',
      description: 'The animation draws training-state objects as detachable compartments. At the export gate, only deterministic forward semantics and required constants may continue to the device.',
      steps: [
        {icon: '🧺', label: 'Assemble a training batch', data: 'samples → batch[B,…]', action: 'Shuffle and augment samples, then assemble one batch', insight: 'A batch affects both gradient estimates and, potentially, training-mode statistics'},
        {icon: '🔄', label: 'Complete a training step', data: 'loss → grad → optimizer state', action: 'Forward, backward, and parameter-update rings rotate in sequence', insight: 'Optimizer momentum and gradients belong to resumable training state, not inference input'},
        {icon: '🔒', label: 'Switch to evaluation semantics', data: 'Dropout=off, BN=running stats', action: 'Random switches stop blinking and the statistics table locks', insight: 'The same layer may execute different formulas in training and inference modes'},
        {icon: '✂️', label: 'Export and optimize', data: 'checkpoint → inference graph', action: 'Gradient and optimizer compartments detach while constant nodes merge', insight: 'Export transforms the graph, so dynamic branches and folding results must be verified'},
        {icon: '📟', label: 'Run forward only on device', data: 'input → runtime → output', action: 'The fixed graph lights up node by node on the target backend', insight: 'The device budget covers only weights, activations, workspace, and I/O'},
        {icon: '🔍', label: 'Align fixed vectors', data: 'Δ = output_source − output_device', action: 'Overlay two output curves and mark the first point outside tolerance', insight: 'Layer-by-layer comparison locates semantic drift more effectively than the final class alone'},
      ],
      loop: 'A new checkpoint replaces the previous deployment artifact only after passing evaluation-semantics, export-graph, and device-output checks. A failure returns to the relevant stage instead of triggering blind retraining.',
    },
    analogyDetail: {
      title: 'From wind-tunnel trials to flight-control software sealed into a drone',
      story: 'Training resembles repeated wind-tunnel flights: engineers change parameters, inject disturbances, record each deviation, and tune the control law. Inference resembles a production drone taking off. It carries only the finalized control tables, sensor interfaces, and required state—not the entire wind tunnel, trial logs, and tuning team. Export is the certification step that turns an experimental configuration into a flashable release.',
      illustration: [
        {icon: '🌬️', label: 'Wind tunnel and disturbances', mapsTo: 'Data batches, augmentation, and training randomness such as Dropout'},
        {icon: '🧑‍🔧', label: 'Tuning engineer', mapsTo: 'The optimizer reads gradients and maintains momentum and other update state'},
        {icon: '📋', label: 'Certified configuration', mapsTo: 'eval mode, checkpoint selection, and freezing the exported graph'},
        {icon: '🛩️', label: 'Onboard flight control', mapsTo: 'The device runtime executes only the verified forward path'},
      ],
      boundary: 'Flight control is usually built from explicit physical models and safety arguments, whereas a neural network is statistical and cannot be assumed to cover every field condition merely because it is “finalized.” Nor does the analogy mean the graph stops changing after export: constant folding, quantization, and backend partitioning still alter numerical and resource behavior.',
    },
  },
  {
    n: 3,
    t: 'Model Formats',
    s: 'Turn a model into a portable intermediate representation',
    goal: 'Learn to read formats such as ONNX, TFLite, and GGUF from three perspectives: computation graph, weight tensors, and metadata.',
    concept: ['Computation graphs and topological ordering', 'Weight layouts and dtypes', 'Opsets, metadata, and version compatibility', 'Responsibilities of converters, runtimes, and backends'],
    analogy: 'A model format resembles a USB descriptor plus a packet protocol: the descriptor declares endpoints and capabilities, weights are the payload, and the runtime maps it all onto a concrete controller.',
    diagram: 'Framework → Export IR → Converter → Optimizer → Runtime → Backend\n PyTorch       ONNX/TFLite/GGUF                     CPU/GPU/NPU',
    code: 'onnx.checker.check_model(model)\nprint(model.graph.input, model.graph.output)\n# For TFLite/GGUF, also inspect quantization types and tokenizer metadata',
    lab: 'Choose a small model and list its inputs, outputs, operators, weight dtypes, dynamic dimensions, and metadata.',
    questions: ['Why can changing the format of the same model change its size and accuracy?', 'When an opset is incompatible, is the problem usually at the exporter or the runtime?', 'Why is metadata more critical for an LLM than for a typical CNN?'],
    next: 'Quantization',
    lesson: [
      {title: 'Separate a format into syntax, semantics, and companion assets', body: 'A schema only says how fields are encoded; operator specifications say what nodes must compute; tokenizers, label maps, and normalization parameters may live in metadata or external files. Successful parsing proves only that the syntax is valid. Missing operator semantics makes a runtime reject the model, while missing assets may let it run but misinterpret its outputs. List validation methods for every layer in the delivery checklist instead of recording only one model-file hash.'},
      {title: 'While reading a graph, track each value’s origin, lifetime, and variability', body: 'Graph inputs come from the caller, initializers usually store constant weights, and intermediate values are produced by nodes; symbolic or unknown values express dynamic dimensions. Topological order gives dependencies but not necessarily final execution order, because a runtime can reuse memory and schedule work in parallel. Mark which path produces each output, which dimensions may vary, and where shape inference fails.'},
      {title: 'IR, opset, and runtime versions are three different locks', body: 'The IR version constrains the model container and graph structure, an opset constrains signatures and semantics in a particular domain, and the runtime version determines implementation coverage. Lowering an opset is not changing an integer: if the older set has no equivalent expression, the converter must decompose a node or fail. A custom domain also requires shipping its implementation. A compatibility matrix should record all three versions with the target chip, not merely say “supports ONNX.”'},
      {title: 'Converters rewrite representations, so audit the change list', body: 'Conversion may expand a high-level operator into a subgraph, precompute constants, change NCHW to NHWC, or store weights in another dtype. Even mathematically equivalent rewrites can alter rounding, workspace, and backend partitioning. Preserve counts of added, removed, and replaced nodes in conversion logs, and map names for important intermediate tensors so an abnormal final output does not leave you guessing.'},
      {title: 'Build a model ABI test bundle instead of saving the model alone', body: 'Attach input names, shape ranges, dtypes, units, preprocessing and postprocessing versions, and several golden samples to every model version. Samples should include normal values, boundary shapes, and invalid inputs. CI first runs schema/checker validation, then loads the model in the target runtime and compares outputs within tolerance. If the model uses external weights or tokenizer files, validate relative paths and hashes as well, preventing a stable main file from hiding drift in companion assets.'},
    ],
    pitfall: 'Being able to open a model in Netron proves only that the file is parseable; it does not prove that the target runtime supports its opset, dynamic dimensions, or custom operators.',
    references: [
      ['ONNX Concepts', 'https://onnx.ai/onnx/intro/concepts.html'],
      ['ONNX Versioning', 'https://onnx.ai/onnx/repo-docs/Versioning.html'],
      ['GGUF Format Specification', 'https://github.com/ggerganov/ggml/blob/master/docs/gguf.md'],
    ],
    quiz: [
      {prompt: 'What does an ONNX opset primarily declare?', options: ['The number of training epochs', 'The semantic version that graph operators must follow', 'The chip clock frequency', 'The weight compression ratio'], answer: 1, explanation: 'An opset is a versioned operator collection. The same Add operator can follow different definitions across opsets, so the runtime must support the declared domain and version.'},
      {prompt: 'Which description of GGUF is most accurate?', options: ['One specific quantization algorithm', 'A model container format for inference in GGML-family runtimes', 'A training framework', 'An NPU driver'], answer: 1, explanation: 'GGUF stores tensors and extensible metadata. Its tensors may use different quantization schemes, so the container format must not be confused with a particular quantization algorithm.'},
      {prompt: 'What is the most reliable first validation after conversion?', options: ['Compare file sizes only', 'Compare source-model and target-runtime outputs using fixed inputs', 'Check only the filename extension', 'Read only the parameter count on the model card'], answer: 1, explanation: 'Fixed test vectors directly reveal changes in I/O layout, numerical type, or operator semantics. File size cannot prove that inference results are correct.'},
    ],
    readingMinutes: 18,
    keywords: [
      {term: 'Computation graph', definition: 'A set of nodes and edges describing how tensors pass through operators to produce outputs.', espAnalogy: 'Like a firmware data-flow graph or task-dependency graph.'},
      {term: 'ONNX opset', definition: 'The version of operator semantics declared by a model.', espAnalogy: 'Like a communication-protocol version: sender and receiver must be compatible.'},
      {term: 'Metadata', definition: 'Names, vocabularies, quantization details, or configuration saved with a model.', espAnalogy: 'Like a USB descriptor that helps the host interpret a payload correctly.'},
      {term: 'Runtime', definition: 'Software that loads a model and schedules its operators for execution.', espAnalogy: 'Like an RTOS plus a driver abstraction layer—not the model file itself.'},
    ],
    recap: 'The previous chapter fixed the model as an inference graph and a set of weights. This chapter examines how that artifact moves among frameworks, converters, and hardware backends, and what can be distorted in transit.',
    nextPreview: 'The next chapter reduces numerical precision while keeping the format contract stable. Quantization scales, zero-points, and calibration data all depend on the tensor semantics confirmed here.',
    history: {
      intro: 'The evolution of model formats is fundamentally the story of handing an object internal to one framework to another software and hardware stack. Early models often depended on training code for interpretation; later, data-flow graphs, common operator semantics, and lightweight binary containers separated those concerns. Formats did not erase differences—they moved them from hidden source-code dependencies into inspectable schemas, opsets, and metadata.',
      milestones: [
        {year: '2014', title: 'Caffe promoted model reuse through declarative networks and weight files', body: 'Caffe organized network structure, training configuration, and learned parameters as exchangeable artifacts, using layers as extension units. It demonstrated that a model could be shared and deployed outside one research script, while also exposing the problem of framework-specific layers tied to a format.', source: {label: 'Original Caffe paper', url: 'https://arxiv.org/abs/1408.5093'}},
        {year: '2015', title: 'TensorFlow expressed training and inference in one data-flow graph', body: 'The TensorFlow white paper represented machine-learning computation on heterogeneous systems as a stateful data-flow graph. Nodes, edges, device placement, and tensor flow became system-level interfaces, providing a clear foundation for later graph optimization, serialization, and cross-device execution.', source: {label: '2015 TensorFlow white paper', url: 'https://research.google/pubs/tensorflow-large-scale-machine-learning-on-heterogeneous-distributed-systems/'}},
        {year: '2017', title: 'ONNX v1 published interoperability as a graph-and-operator contract', body: 'ONNX v1 released a production-ready format for multiple frameworks, promoting model transfer through an open graph format and operator sets. Exporters, converters, and runtimes could now collaborate around a common IR, although each still had to support the domains and opsets declared by a model.', source: {label: 'Official ONNX v1 release announcement', url: 'https://engineering.fb.com/2017/12/08/ml-applications/onnx-v1-released/'}},
        {year: '2017', title: 'TensorFlow Lite tightened format boundaries for mobile and embedded execution', body: 'TensorFlow Lite targeted small binaries, fast startup, and on-device acceleration. Model conversion was no longer only for interchange; it prepared structures that a constrained runtime could read directly. Supported operators, static memory planning, and hardware delegation began to influence model design upstream.', source: {label: 'Official TensorFlow Lite announcement', url: 'https://developers.googleblog.com/en/announcing-tensorflow-lite/'}},
        {year: '2023', title: 'GGUF emphasized single-file packaging, extensibility, and fast loading', body: 'GGUF, a model format for GGML-family runtimes, placed tensors and key-value metadata in an extensible binary container and superseded earlier GGML, GGMF, and GGJT formats. It showed that LLM deployment must preserve not just weights, but also reliable interpretation data such as architecture and tokenizer metadata.', source: {label: 'Official GGUF specification', url: 'https://github.com/ggml-org/ggml/blob/master/docs/gguf.md'}},
      ],
      bridge: 'Modern formats follow two complementary paths. IRs such as ONNX emphasize general computation graphs and operator semantics, while deployment containers such as LiteRT and GGUF stay closer to the loading and resource constraints of particular runtimes. Engineering choices should begin not with a file extension, but with the target backend’s support matrix, the input contract, variable shapes, and assets that must travel with the model.',
    },
    visual: {
      title: 'A model passes through five format gates',
      description: 'The same test data travels with the model from its training framework to the target backend. Each gate checks one type of contract, and any red light stops the journey near its root cause.',
      steps: [
        {icon: '🧩', label: 'Framework object', data: 'modules + parameters + control flow', action: 'Expand the modules actually executed and their parameter references', insight: 'The source program may contain dynamic behavior that cannot be serialized directly'},
        {icon: '🕸️', label: 'Export computation graph', data: 'nodes + edges + initializers', action: 'Capture control paths and connect tensor dependencies into a directed graph', insight: 'The example input may determine which branches and shapes are captured'},
        {icon: '📜', label: 'Check specifications', data: 'IR vN + domain/opset', action: 'Look up every node’s signature and semantics in the version registry', insight: 'Parsing a file does not mean the runtime implements every operator'},
        {icon: '🔁', label: 'Convert deployment format', data: 'layout/dtype/metadata changes', action: 'Fold, split, or relayout nodes while updating the change table', insight: 'Conversion is a semantics-preserving rewrite, not a byte-for-byte copy'},
        {icon: '🧱', label: 'Partition in the runtime', data: 'supported subgraphs → backend', action: 'Route supported nodes to the accelerator and mark fallback paths for the rest', insight: 'Even after format compatibility, kernel, copy, and memory constraints remain'},
        {icon: '✅', label: 'Accept with fixed inputs', data: 'golden input → Δoutput', action: 'Overlay source and target outputs, showing error and the first divergent node', insight: 'Numerical evidence closes the handoff claim that this is “the same model”'},
      ],
      loop: 'When a runtime, opset, converter, or companion asset changes, replay from the affected gate. Golden inputs always travel with the model, forming a regression-ready format passport.',
    },
    analogyDetail: {
      title: 'Containers, manifests, and port rules in international shipping',
      story: 'A model is like a shipment of precision equipment changing carriers across countries. Weights are the cargo, the computation graph is the unloading and connection plan, metadata is the manifest, and the opset is the current edition of handling rules. Arriving at port only means the container can be identified; the destination still needs matching cranes and trained crews to assemble everything correctly.',
      illustration: [
        {icon: '📦', label: 'Shipping container', mapsTo: 'The binary model file packages tensors and structure'},
        {icon: '🗺️', label: 'Unloading route map', mapsTo: 'The computation graph defines node dependencies and tensor flow'},
        {icon: '📑', label: 'Manifest and rulebook', mapsTo: 'Metadata, IR version, opset, and custom domains'},
        {icon: '🏗️', label: 'Port cranes', mapsTo: 'The runtime and backend must implement the required operators and dtypes'},
      ],
      boundary: 'Real model conversion does not move a sealed container unchanged. A converter may alter layouts, split operators, fold constants, or quantize tensors. The shipping analogy also cannot express floating-point non-associativity or dynamic control flow, so “all cargo present” never replaces numerical comparison with fixed inputs.',
    },
  },
  {
    n: 4,
    t: 'Quantization',
    s: 'Trade fewer bits for lower storage and bandwidth',
    goal: 'Master symmetric and asymmetric quantization, scale and zero-point, and learn to identify sources of error.',
    concept: ['Mapping between int8 and float', 'Per-tensor and per-channel quantization', 'Calibration sets and activation ranges', 'Quantization error, saturation, and dequantization'],
    analogy: 'It is like mapping high-precision sensor readings onto a finite set of ADC codes: scale is the range, zero-point is the zero reference, and the calibration set determines whether that range covers real signals.',
    diagram: 'float x ──(x/scale)+zp──► int8 q ──runtime kernel──► int8/float output',
    code: 'q = round(x / scale + zero_point)\nq = clip(q, -128, 127)\nx_hat = (q - zero_point) * scale',
    lab: 'Write a NumPy/Python quantizer, compare the MSE of per-tensor and per-channel schemes, and calculate the saturation rate.',
    questions: ['Why are weights often quantized per channel while activations are often quantized per tensor?', 'Why can a calibration set not consist of one “average image”?', 'Does quantization always make inference faster?'],
    next: 'Operators',
    lesson: [
      {title: 'Start with the approximately reversible formula to expose three error entry points', body: 'Affine quantization uses q=round(x/scale)+zero_point and then clamps q to the integer range; dequantization recovers only an approximation on that grid. Error comes from rounding at finite spacing, saturation when the range is too narrow, and wasted resolution when outliers stretch the statistical range. Measure quantization MSE, lower/upper-bound hit rates, and the zero mapping separately. Folding them into one accuracy number makes diagnosis difficult.'},
      {title: 'Symmetric, asymmetric, and per-channel schemes are backend contract choices', body: 'Symmetric quantization usually sets zero-point to zero for a simpler multiply path; asymmetric quantization uses more of the integer range for skewed distributions. Per-channel quantization assigns a scale to each output channel and often handles cross-channel differences in weight magnitude better, but its kernel must know the quantization axis and load multiple parameter sets. Before choosing, inspect the target runtime’s dtype, axis, and operator constraints rather than relying on PC accuracy.'},
      {title: 'Calibration is not sampling a few “typical images”; it must cover activation state space', body: 'Weight ranges can be scanned directly, but intermediate activations depend jointly on inputs and preceding layers. Representative data should span lighting, silence, saturated sensors, class boundaries, and device noise, entering through the real preprocessing path. Inspect per-layer histograms and clipping rates to see whether a few outliers dominate the scale. If the production distribution changes, treat the old calibration table as a versioned asset that needs revalidation.'},
      {title: 'Integer convolution still needs wide accumulation and correct scale handoffs', body: 'Products of int8 inputs and int8 weights usually enter a wider accumulator. The bias scale should align with input scale × weight scale, and the output is requantized using a multiplier and shift or an equivalent operation. Any difference in zero-point compensation, rounding rule, or saturation order can introduce systematic bias. Hand-calculating accumulation and boundary values for a tiny matrix is one of the most effective unit tests for a custom kernel.'},
      {title: 'Put accuracy regression and performance acceptance in one layer-level report', body: 'First compare final task metrics between float and quantized models. For samples with the largest output drift, locate per-layer SQNR, cosine similarity, or maximum absolute error; at the same time, inspect the profile for Quantize/Dequantize islands and float fallback. If the file becomes smaller but execution slower, the cause may be missing integer kernels, layout conversion, or frequent rescaling—not a failure of quantization itself.'},
    ],
    pitfall: 'Quantizing to int8 does not guarantee speed. If the target backend lacks int8 kernels, or frequently inserts dequantize/quantize operations, conversion overhead can erase the benefit.',
    references: [
      ['LiteRT Post-Training Quantization', 'https://ai.google.dev/edge/litert/models/post_training_quantization'],
      ['ESP-DL Quantization', 'https://docs.espressif.com/projects/esp-dl/en/latest/tutorials/how_to_quantize_model.html'],
    ],
    quiz: [
      {prompt: 'Why does full-integer quantization require a representative dataset?', options: ['To retrain all weights', 'To estimate activation ranges that vary with the input', 'To generate the model name', 'To increase camera frame rate'], answer: 1, explanation: 'Weights can be scanned directly. Inputs and intermediate activations require representative runs to estimate min/max or other range statistics.'},
      {prompt: 'Which ADC problem most closely resembles quantization saturation?', options: ['Sampling too quickly', 'The input exceeds the range and is clamped to the largest or smallest code', 'A mismatched UART baud rate', 'Erasing Flash'], answer: 1, explanation: 'Values outside the range covered by the scale are clipped to int8 boundaries; after dequantization, their original differences are already lost.'},
      {prompt: 'According to ESP-DL documentation, which strategy is commonly used for ESP32 and ESP32-S3?', options: ['Every Conv must be per-channel', 'Per-tensor quantization', 'Only float32 is supported', 'No quantization is supported'], answer: 1, explanation: 'ESP-DL platform strategies are constrained by the ISA and implementation. Always check the target chip instead of generalizing ESP32-P4 per-channel capabilities to every ESP32.'},
    ],
    readingMinutes: 19,
    keywords: [
      {term: 'Scale', definition: 'The proportional factor between an integer code and its real value.', espAnalogy: 'Like an ADC range and the physical meaning of one code step.'},
      {term: 'Zero-point', definition: 'The integer offset that represents real zero in the mapping.', espAnalogy: 'Like the reference code after correcting a sensor’s zero offset.'},
      {term: 'Calibration set', definition: 'Representative input samples used to estimate activation ranges.', espAnalogy: 'Like calibrating an ADC with real operating conditions instead of one midpoint input.'},
      {term: 'Per-channel', definition: 'Using separate quantization parameters for different weight channels.', espAnalogy: 'Like calibrating gain independently for each sensor channel.'},
    ],
    recap: 'The previous chapter established the model format, operator semantics, and I/O contract. Keeping those contracts fixed, this chapter maps float values into fewer bits and quantifies the sources of error.',
    nextPreview: 'The next chapter follows the quantized graph into operators and kernels. The same int8 graph can have completely different latency under different layouts, fusions, and backends.',
    history: {
      intro: 'Neural-network quantization did not simply appear in response to mobile chips. Early digital and analog neural hardware already faced finite word lengths. As deep learning enlarged models, research moved from asking whether finite precision could work to co-designing compression, low-bit training, and integer operators. Today’s PTQ, QAT, per-channel methods, and representative calibration are engineering outcomes of decades spent balancing precision, storage, and hardware.',
      milestones: [
        {year: '1990', title: 'Finite word length became a neural-computing system design problem', body: 'Neural Network Number Systems compared fixed-point, floating-point, and logarithmic number representations for digital neural networks. Early work already recognized that bit width affects circuit cost, dynamic range, and computational error together; it is not merely a change of file dtype.', source: {label: 'Record of the original Neural Network Number Systems paper', url: 'https://stars.library.ucf.edu/scopus1990/1465/'}},
        {year: '2015', title: 'Deep Compression placed quantization in a complete model-compression pipeline', body: 'Deep Compression combined pruning, post-training weight-sharing quantization, and Huffman coding, demonstrating substantial reductions in model storage and memory traffic. It reinforced an engineering fact: a compression method must be evaluated together with encoding overhead, its decode path, and target hardware.', source: {label: 'Original Deep Compression paper', url: 'https://arxiv.org/abs/1510.00149'}},
        {year: '2016–2017', title: 'Low-bit weights and activations entered the training process', body: 'Quantized Neural Networks introduced low-precision weights and activations into forward and backward computation and studied extremely low-bit representations. Quantization was no longer packaging applied after training; training could expose its error explicitly so parameters adapted to a discrete codebook.', source: {label: 'Original Quantized Neural Networks paper', url: 'https://www.jmlr.org/papers/v18/16-456.html'}},
        {year: '2018', title: 'Integer arithmetic ran from input through output', body: 'Jacob and colleagues presented a quantization scheme and training flow for integer-arithmetic inference, quantizing weights and activations while handling scale factors, zero-points, and accumulation. Algorithm design explicitly connected to ARM CPUs and integer accelerators instead of reporting only a smaller file.', source: {label: 'Original CVPR integer-inference paper', url: 'https://openaccess.thecvf.com/content_cvpr_2018/html/Jacob_Quantization_and_Training_CVPR_2018_paper.html'}},
        {year: '2020s', title: 'PTQ calibration became a standard stage in edge toolchains', body: 'LiteRT full-integer quantization uses representative data to estimate the ranges of inputs and intermediate activations, and can make model I/O integer as well. Current practice therefore emphasizes calibration data, operator coverage, and measurement on target hardware rather than treating an int8 label as a performance guarantee.', source: {label: 'Official LiteRT full-integer quantization tutorial', url: 'https://ai.google.dev/edge/litert/models/post_training_integer_quant'}},
      ],
      bridge: 'The historical path runs from “can finite precision work?” to co-design across training, formats, kernels, and chips. On an ESP32-class platform, the right question is not simply how many bits can replace float, but which quantization axes, accumulator widths, and operator combinations the target ISA supports, and whether calibration samples cover the long tail of real sensor values.',
    },
    visual: {
      title: 'How a floating-point waveform lands on 256 integer rungs',
      description: 'The animation first stretches a ruler over measured data, then snaps each floating-point value to its nearest rung. Out-of-range points hit red barriers before the integer stream enters an accumulator.',
      steps: [
        {icon: '📊', label: 'Scan the distribution', data: 'activation samples → min/max/histogram', action: 'Spread samples into a histogram and color the long tail separately', insight: 'Activation ranges must be estimated from representative real inputs'},
        {icon: '📏', label: 'Set grid spacing', data: 'scale, zero_point', action: 'Shift and stretch the integer ruler over the chosen range', insight: 'Scale controls resolution; zero-point chooses the code representing real zero'},
        {icon: '🧲', label: 'Round to grid points', data: 'q = round(x/scale)+zp', action: 'Snap every floating-point point to its nearest integer tick', insight: 'Sub-grid detail becomes irrecoverable rounding error here'},
        {icon: '🧱', label: 'Apply saturation', data: 'q = clip(q,qmin,qmax)', action: 'Out-of-range points hit red barriers and pile up at both ends', insight: 'Saturation permanently maps several distinct originals to one code'},
        {icon: '⚙️', label: 'Integer kernel', data: 'int8 × int8 → int32 accumulate', action: 'Integer codes enter the multiply–accumulate array while the accumulator slot widens', insight: 'Low-bit inputs do not mean every intermediate uses the same width'},
        {icon: '🔬', label: 'Rescale and compare', data: 'requantize → q_out; Δ vs float', action: 'Return output to the target scale and overlay it with the float curve', insight: 'Check numerical error, saturation rate, and real kernel latency together'},
      ],
      loop: 'If error or saturation exceeds the limit, return to distribution scanning and change calibration samples, quantization axis, or range strategy. If performance misses its target, return to the integer kernel and inspect fallback and conversion boundaries.',
    },
    analogyDetail: {
      title: 'Printing a continuous mountain scene with a limited set of tones',
      story: 'A photographer has an original rich in subtle gradations, but the press offers only a finite number of levels for each color. The plate maker studies the darkest and brightest areas across the whole collection before choosing the range covered by one tone. Colors between levels must be rounded, while highlights and shadows outside the paper’s range collapse to the same extrema. Separate color plates resemble choosing an independent range for each channel.',
      illustration: [
        {icon: '🏔️', label: 'Original mountain scene', mapsTo: 'Continuous or high-precision float weights and activations'},
        {icon: '🎨', label: 'Limited palette', mapsTo: 'The integer codebook and spacing determined by scale'},
        {icon: '🖼️', label: 'Separate color plate', mapsTo: 'Per-channel parameters for different output channels'},
        {icon: '☀️', label: 'Blown highlights', mapsTo: 'Values outside the calibrated range are clipped and cannot be recovered'},
      ],
      boundary: 'Printed tonal differences are judged visually, whereas model error accumulates through many operators and must be measured by task metrics, not by whether it “looks similar.” Real integer kernels also involve bias scales, int32 accumulation, requantization, and hardware instructions that the printmaking analogy cannot explain.',
    },
  },
  {
    n: 5,
    t: 'Operators',
    s: 'Move from mathematical definitions to executable kernels',
    goal: 'Understand the relationship among operator semantics, memory layouts, kernel selection, and operator fusion.',
    concept: ['Conv/GEMM/Softmax/LayerNorm', 'Operator input/output contracts', 'NHWC/NCHW and contiguity', 'Fusion, tiling, and SIMD'],
    analogy: 'An operator is one command in a protocol stack; a kernel is the chip-specific driver implementation. The same command can have different hardware backends.',
    diagram: 'Graph: Conv → BN → ReLU\nOptimizer: Conv+BN+ReLU fusion\nKernel: tile → load → MAC → store',
    code: 'for (m_tile : M)\n  for (n_tile : N)\n    acc = 0\n    for (k_tile : K) acc += A * B\n    C = acc + bias',
    lab: 'Derive the equivalence between a 1×1 Conv and GEMM; record transposes, cache misses, and memory-access counts before and after fusion.',
    questions: ['Why is operator fusion often more important than reducing FLOPs?', 'When can layout conversion consume all the gain from a faster kernel?', 'How should a runtime fall back when the NPU does not support an operator?'],
    next: 'Non-LLM Models',
    lesson: [
      {title: 'An operator contract includes attributes, boundaries, and numerical conventions', body: 'Conv is more than a summation formula. Its contract includes stride, padding, dilation, groups, weight-dimension order, and output-shape rules; Softmax must specify both its normalization axis and numerically stable evaluation. Two runtimes may claim support for the same named operator while covering different dtype or attribute combinations. Export a contract table from actual nodes during integration, and validate it with asymmetric shapes and boundary inputs.'},
      {title: 'One Conv can map to several algorithms with different workspace tradeoffs', body: 'Direct convolution, im2col+GEMM, Winograd, and hardware-specific paths suit different regions. im2col reuses mature matrix-multiplication kernels but can expand into a large buffer; Winograd can reduce some multiplications but is sensitive to dimensions, numerical precision, and transform overhead. Measure backend algorithm choices with real shapes, available workspace, and the actual quantization mode instead of comparing theoretical MACs alone.'},
      {title: 'Layout determines which values are adjacent—and how smoothly vector units are fed', body: 'NCHW and NHWC change adjacency among channel and spatial dimensions. Even when dimension names agree, non-contiguous strides may trigger an implicit copy. One kernel may prefer contiguous channels while another accelerator requires a blocked layout, causing boundary conversion to read and write the entire activation. Treat transpose, reorder, and memcpy as first-class timed nodes in a profile, not unexplained framework overhead.'},
      {title: 'Tiling aims to make reuse happen in the fastest storage level', body: 'After partitioning M, N, and K, GEMM loads small input and weight tiles into registers or cache, performs repeated MACs, and only then writes back. A tile that is too large overflows the working set; one that is too small increases loop and boundary overhead. SIMD also imposes alignment and needs a mask or scalar path for tail elements. Test real addresses, alignments, and DMA origins on ESP32, because the ideal contiguity of desktop arrays does not appear automatically.'},
      {title: 'Accept fusion and accelerator partitioning by counting fewer materializations', body: 'Fusing Conv, Bias, and ReLU can retain intermediates in registers or on-chip SRAM. Fusion may be restricted, however, if another node consumes an intermediate or neighboring quantization scales are incompatible. NPU subgraph boundaries likewise introduce synchronization, layout changes, and copies. An acceptance report should list nodes before and after fusion, partition boundaries, bytes moved, and end-to-end time, rather than substituting a single-kernel speedup for system benefit.'},
    ],
    pitfall: 'Interleaving unsupported NPU subgraphs with CPU fallback creates copies at partition boundaries. Peak NPU TOPS alone cannot predict end-to-end speed.',
    references: [
      ['ONNX Concepts', 'https://onnx.ai/onnx/intro/concepts.html'],
      ['ONNX Runtime Execution Providers', 'https://onnxruntime.ai/docs/execution-providers/'],
    ],
    quiz: [
      {prompt: 'What is the main reason operator fusion often improves speed?', options: ['It changes the training labels', 'It reduces intermediate-tensor traffic and scheduling overhead', 'It increases the batch without limit', 'It automatically enlarges NPU SRAM'], answer: 1, explanation: 'Fusion usually preserves mathematical semantics while avoiding writes of intermediate results to slow memory. On bandwidth-bound devices, this is often more important than a small reduction in FLOPs.'},
      {prompt: 'What is the risk of an NCHW-to-NHWC conversion?', options: ['It changes only the file extension', 'It may copy a full tensor and break contiguous layout', 'It always improves accuracy', 'It eliminates every dynamic shape'], answer: 1, explanation: 'A layout transform is real memory traffic. When placed on both sides of an accelerated kernel, copy cost may exceed the kernel’s gain.'},
      {prompt: 'How do multiple ONNX Runtime Execution Providers commonly handle fallback?', options: ['Delete unsupported nodes', 'Assign supported subgraphs to EPs by priority and let the CPU execute the rest', 'Retrain the model', 'Copy every node into Flash'], answer: 1, explanation: 'An EP claims nodes or subgraphs after capability discovery. The CPU provider commonly takes unsupported portions, so partitioning and transfers must be measured.'},
    ],
    readingMinutes: 18,
    keywords: [
      {term: 'Kernel', definition: 'Low-level code that implements an operator for specific hardware.', espAnalogy: 'Like a chip-specific driver implementing a shared peripheral API.'},
      {term: 'Layout', definition: 'The arrangement of tensor dimensions in memory, such as NCHW or NHWC.', espAnalogy: 'Like structure-field ordering and alignment rules.'},
      {term: 'Fusion', definition: 'Combining several consecutive operators into one execution.', espAnalogy: 'Like merging several small DMA transactions to reduce interrupts and traffic.'},
      {term: 'Tiling', definition: 'Partitioning a large computation to fit registers or cache.', espAnalogy: 'Like processing a long data stream with block-sized buffers.'},
    ],
    recap: 'The previous chapter produced graphs and tensors with quantization constraints. This chapter lowers their mathematical operators into real kernels and explains why identical FLOP counts can behave very differently on an edge device.',
    nextPreview: 'The next chapter combines operators, quantization, and I/O into a complete deployment loop for a small non-LLM model, from data collection through an ESP32 event output.',
    history: {
      intro: 'Operator history intertwines two paths: inventing network structures that can express a task, and turning those mathematical structures into efficient kernels. After convolutional networks progressed from hierarchical receptive fields to end-to-end training, GPU primitive libraries separated optimized implementations from frameworks, and compilers automated fusion, layout, and scheduling. A modern runtime sits precisely between the mathematical graph and heterogeneous hardware.',
      milestones: [
        {year: '1980', title: 'The Neocognitron demonstrated hierarchical local receptive fields and shift robustness', body: 'Fukushima’s Neocognitron processed visual patterns hierarchically and pursued recognition under position changes. It was not a modern backpropagation-trained Conv kernel, but it brought local connectivity, feature hierarchies, and spatial reuse into the convolutional-network lineage.', source: {label: 'Original Neocognitron paper', url: 'https://pubmed.ncbi.nlm.nih.gov/7370364/'}},
        {year: '1998', title: 'LeNet joined convolution, subsampling, and gradient training in an application system', body: 'LeCun and colleagues’ document-recognition work systematically demonstrated convolutional networks and end-to-end gradient learning. Operators were no longer isolated formulas; together with input geometry, weight sharing, and task postprocessing, they formed an executable pipeline.', source: {label: 'Original LeNet document-recognition paper', url: 'https://bottou.org/papers/lecun-98h'}},
        {year: '2014', title: 'cuDNN made deep-learning primitives a reusable high-performance library', body: 'cuDNN provided optimized GPU primitives such as convolution so frameworks did not need to rewrite every kernel for each generation of parallel hardware. Its paper compared deep-learning operator libraries with BLAS and showed how algorithm selection, memory use, and hardware optimization could hide behind stable interfaces.', source: {label: 'Original cuDNN paper', url: 'https://arxiv.org/abs/1410.0759'}},
        {year: '2018', title: 'TVM unified graph fusion and low-level scheduling in compilation search', body: 'TVM handled high-level operator fusion, hardware-intrinsic mapping, and memory-latency hiding while using cost models to search low-level optimizations. Kernel selection expanded from vendor-written libraries into a schedule space generated for CPUs, GPUs, FPGAs, and accelerators.', source: {label: 'Original TVM OSDI paper', url: 'https://www.usenix.org/conference/osdi18/presentation/chen'}},
        {year: '2020s', title: 'Execution Providers partitioned one graph among heterogeneous backends by capability', body: 'ONNX Runtime connects hardware implementations through Execution Providers and assigns supported nodes or subgraphs to each backend. Modern deployment therefore asks not only whether an NPU exists, but which nodes it claims, how much data crosses boundaries, and which paths fall back to the CPU.', source: {label: 'Official ONNX Runtime Execution Providers documentation', url: 'https://onnxruntime.ai/docs/execution-providers/'}},
      ],
      bridge: 'From architectural inventions to kernel libraries, graph compilers, and heterogeneous partitioning, the central goal has remained preserving operator semantics while eliminating unproductive data movement. Edge optimization must begin from actual shapes, layouts, quantization parameters, and support matrices. Peak TOPS can materialize only when the subgraph is sufficiently complete and its data already resides in the right memory.',
    },
    visual: {
      title: 'How Conv–BN–ReLU becomes one tiled execution',
      description: 'The animation descends from an abstract graph into memory: first verify operator contracts, then choose fusion and layout, and finally cycle tiles through load, compute, and write-back in on-chip buffers.',
      steps: [
        {icon: '🧾', label: 'Expand operator contracts', data: 'Conv(stride,pad,groups) → BN → ReLU', action: 'Attach attribute cards to nodes and update input/output shapes together', insight: 'Same-name operators are semantically compatible only when attributes, axes, and dtypes also match'},
        {icon: '🧬', label: 'Identify the fusible region', data: 'Conv + folded BN + activation', action: 'Collapse fusible nodes into one colored subgraph while preserving bypass boundaries', insight: 'Fusion depends on graph structure, quantization scales, and backend capabilities'},
        {icon: '🧱', label: 'Choose a memory layout', data: 'NCHW ⇄ NHWC / blocked', action: 'Rearrange tensor cells and display the byte count for one conversion', insight: 'A layout can help a kernel while still creating a full-tensor copy at a boundary'},
        {icon: '📥', label: 'Load one tile', data: 'input tile + weight tile → local SRAM/cache', action: 'Slide a small data block from slow memory into on-chip workspace', insight: 'Tile dimensions determine reuse, cache hits, and peak workspace'},
        {icon: '⚙️', label: 'MAC and activate in place', data: 'acc += A×B; acc=max(acc,0)', action: 'SIMD lanes flash in parallel and the accumulated result passes directly through the activation gate', insight: 'The primary gain from fusion is keeping intermediates out of slow memory'},
        {icon: '📤', label: 'Write back and profile', data: 'output tile; bytes/time/fallback', action: 'Write each result once while counters accumulate traffic and time', insight: 'End-to-end evidence must include copies, synchronization, and unclaimed nodes'},
      ],
      loop: 'Tiles iterate across the output space until completion. Changing layout, tile size, or backend resets counters while retaining the previous run’s bytes, workspace, and latency side by side.',
    },
    analogyDetail: {
      title: 'How one recipe is executed in a crowded professional kitchen',
      story: 'Operators are recipe instructions such as dice, sauté, and reduce: they specify inputs, actions, and results. A kernel is a particular cook’s technique on a particular stove. Arranging ingredients in order of use is layout; dicing and marinating on the same board is fusion; dividing a huge batch into portions that fit the pan is tiling. The recipe stays the same, yet serving time can differ by several times.',
      illustration: [
        {icon: '📖', label: 'Standard recipe', mapsTo: 'Operator name, attributes, inputs/outputs, and numerical semantics'},
        {icon: '👨‍🍳', label: 'Cook and stove', mapsTo: 'Different kernel implementations for CPU, GPU, and NPU'},
        {icon: '🥬', label: 'Preparation trays', mapsTo: 'NHWC/NCHW, contiguity, alignment, and the buffer’s memory location'},
        {icon: '🍳', label: 'Small-pan batches', mapsTo: 'Tiling fits the working set into registers or cache'},
      ],
      boundary: 'The kitchen analogy cannot represent parallel threads, vector instructions, or floating-point rounding, and may imply that fusion is always beneficial. Real fusion is constrained by quantization scales, branch reuse, and backend support; both numerical equivalence and performance gain must be verified on target hardware.',
    },
  },
  {
    n: 6,
    t: 'Training and Deploying Non-LLM Edge Models',
    s: 'Put vision, audio, and sensor models onto a real device',
    goal: 'Complete the loop from data, training, export, and quantization to deployment and validation, with an emphasis on small models commonly used on MCUs.',
    concept: ['Dataset splits and augmentation', 'Selecting small-model architectures', 'Representative-dataset calibration', 'TFLite Micro/ESP-DL deployment', 'On-device preprocessing, postprocessing, and thresholds'],
    analogy: 'The model is only one firmware module. Sample rate, DMA buffers, task priorities, and the watchdog shape the product experience just as much as accuracy.',
    diagram: 'Sensor → DMA/RingBuffer → Preprocess → Inference → Postprocess → Event/Actuator',
    code: 'idf.py build flash monitor\n# On-device concerns: arena, tensor arena, input normalization, inference latency\nESP_LOGI(TAG, "latency=%d ms", elapsed_ms);',
    lab: 'Choose IMU, audio, or images and build a 2–4 class classifier. Compare input normalization, latency, and confusion matrices between a PC and an ESP32.',
    questions: ['Why does reduced on-device accuracy often come from inconsistent preprocessing?', 'How do failures caused by an undersized arena differ from those caused by an undersized stack?', 'When should a model run on a Linux host instead of the ESP32?'],
    next: 'Memory and Bandwidth',
    lesson: [
      {title: 'Work backward from the product event to the data, not forward from a convenient dataset', body: 'First write down the event the device must publish, tolerable false negatives and false positives, the response deadline, and what happens when no decision is possible. Use those requirements to define sampling windows and labels. Split training data by device or collection batch so adjacent portions of one continuous signal cannot leak into both training and test sets. Negative classes need more than “quiet background”; include confusing actions, noise, and sensor faults. Preserve firmware version, sample rate, and environment metadata on every sample so regressions can locate data drift.'},
      {title: 'Turn preprocessing into a verifiable binary contract', body: 'The PC training pipeline and firmware must agree item by item on sample rate, channel order, crop window, interpolation, color space, normalization constants, rounding, and saturation. Do not compare only the final class. Select golden samples and calculate summaries and spot-check elements at three boundaries: raw buffer, preprocessed float tensor, and quantized integer tensor. If the deployment tool expects int8 input, confirm the direction of scale and zero-point so already-quantized data is not quantized again.'},
      {title: 'Treat export and conversion as a compiler chain, not Save As', body: 'A training checkpoint first switches to inference semantics and exports a graph with a fixed I/O contract. The converter then folds constants, replaces operators, quantizes, and produces a target format. Every stage may change layout or values, so pin tool versions, save operator lists, I/O names, and quantization parameters, and compare the same inputs stage by stage. Successful conversion proves only that a file can be produced; the target can still fail from a missing kernel, float fallback, or an out-of-range shape.'},
      {title: 'Make inference a firmware task governed by scheduling constraints', body: 'The device must schedule sensor producers, DMA, preprocessing, inference, and event consumers together. Use bounded queues with an explicit full-queue policy: dropping old frames is usually more appropriate for real-time work than waiting forever. If inference occupies the CPU for too long, partition it, reduce frequency, or service the watchdog—but never use watchdog feeding to hide a deadlock. Allocate arenas, the model, and I/O buffers during initialization where possible, avoid fragmentation in the steady-state path, and make every error branch return its frame and publish an observable degraded state.'},
      {title: 'Complete deployment acceptance with four layers of evidence', body: 'Compare the source framework, exported model, target runtime, and physical board in sequence, using fixed golden vectors and full replays of raw captures. Results should include at least a confusion matrix, per-class threshold curves, p50/p95 latency, peak arena use, long-running tests, and abnormal inputs. At the business layer, verify hysteresis, consecutive-hit counts, and cooldowns so scores near a threshold do not become an event storm. Rerun the same checklist after every model or SDK update to separate accuracy regressions from system regressions.'},
    ],
    pitfall: 'A common cause of sudden accuracy loss is feeding float images on the PC while firmware incorrectly supplies RGB565 or omits normalization.',
    references: [
      ['Getting Started with ESP-DL', 'https://docs.espressif.com/projects/esp-dl/en/latest/getting_started/readme.html'],
      ['ESP-DL Model Loading, Testing, and Profiling', 'https://docs.espressif.com/projects/esp-dl/en/latest/tutorials/how_to_load_test_profile_model.html'],
      ['LiteRT for Microcontrollers', 'https://ai.google.dev/edge/litert/microcontrollers/get_started'],
    ],
    quiz: [
      {prompt: 'When on-device accuracy is substantially lower than PC accuracy, what should you compare first?', options: ['The webpage CSS', 'Preprocessing: color, resize, normalization, and quantization', 'The model file modification time', 'The Wi-Fi SSID'], answer: 1, explanation: 'Identical weights give different results under different input distributions. Align preprocessing item by item with saved test vectors.'},
      {prompt: 'What does an insufficient tensor arena usually mean?', options: ['The model must have overfit during training', 'Inputs, outputs, or intermediate tensors cannot be fully allocated', 'The Flash partition table was deleted', 'The network API returned 404'], answer: 1, explanation: 'LiteRT Micro places tensors in a preallocated arena. Shortage appears during AllocateTensors or around execution and is a peak-memory budgeting problem.'},
      {prompt: 'When should a general-purpose LLM run on a Linux Edge Host?', options: ['Whenever the ESP32 has GPIO', 'When model weights and KV cache exceed MCU RAM or compute budgets', 'Whenever a temperature sensor exists', 'Whenever USB is used'], answer: 1, explanation: 'The ESP32 can keep acquisition, protocols, and safety control, while general-purpose LLM weights and KV cache usually require a Linux host or a suitable accelerator.'},
    ],
    readingMinutes: 19,
    keywords: [
      {term: 'Representative data', definition: 'A dataset covering real device operating conditions.', espAnalogy: 'Like system-level tests spanning temperature, humidity, power, and load boundaries.'},
      {term: 'Tensor arena', definition: 'A memory region preallocated for MCU inference inputs, outputs, and intermediate tensors.', espAnalogy: 'Like a static workspace planned at startup.'},
      {term: 'Preprocessing', definition: 'The steps that transform raw sensor data into model input.', espAnalogy: 'Like framing, byte order, and validation in a protocol stack.'},
      {term: 'Confusion matrix', definition: 'A table counting predicted classes against true classes.', espAnalogy: 'Like counting false alarms and missed detections by fault category.'},
    ],
    recap: 'The previous chapter showed that operators and kernels ultimately execute the graph. This chapter places those pieces back into the complete product path to deliver a deployable vision, audio, or sensor model.',
    nextPreview: 'The next chapter explains why merely getting a model to run is not enough: Flash, SRAM, PSRAM, bandwidth, and DMA constraints determine whether it can run reliably over time.',
    history: {
      intro: 'Putting a small model on an MCU is not a scaled-down copy of desktop inference code. It followed a shared evolution toward architectures requiring less compute, kernels fitting hardware more closely, runtimes with fewer dependencies, and repeatable product validation—eventually forming today’s closed loop from data to firmware.',
      milestones: [
        {year: '1989', title: 'Structured convolutional networks reached real tasks', body: 'LeCun and colleagues applied local connectivity, weight sharing, and backpropagation to postal-code recognition. They showed that networks could process pixels directly while structural constraints reduced parameter and compute requirements.', source: {label: 'Original paper by LeCun and colleagues', url: 'https://doi.org/10.1162/neco.1989.1.4.541'}},
        {year: '2017', title: 'MobileNet put the hardware budget into network design', body: 'MobileNet used depthwise separable convolutions plus width and resolution multipliers to trade accuracy against latency explicitly. “Set the device budget before selecting a model” moved from an experienced habit to a tunable design method.', source: {label: 'Original MobileNet paper', url: 'https://arxiv.org/abs/1704.04861'}},
        {year: '2017', title: 'Production ML began to emphasize system testing', body: 'Google’s ML Test Score decomposed data, features, models, infrastructure, and monitoring into concrete test items, showing that offline metrics are only one part of production readiness.', source: {label: 'Original Google Research paper', url: 'https://research.google/pubs/the-ml-test-score-a-rubric-for-ml-production-readiness-and-technical-debt-reduction/'}},
        {year: '2018', title: 'CMSIS-NN brought quantized models close to Cortex-M', body: 'CMSIS-NN supplied optimized Cortex-M kernels for convolution, fully connected layers, and more, using techniques such as partial im2col to reduce both runtime and peak memory.', source: {label: 'Original CMSIS-NN paper', url: 'https://arxiv.org/abs/1801.06601'}},
        {year: '2020', title: 'TFLite Micro established a microcontroller runtime pattern', body: 'TFLite Micro targeted resource-constrained, fragmented platforms without virtual memory. A small interpreter and preplanned workspace allowed one model to enter many kinds of MCU.', source: {label: 'Original TFLite Micro paper', url: 'https://arxiv.org/abs/2010.08678'}},
        {year: 'Present', title: 'ESP-DL connects quantization, formats, memory planning, and analysis', body: 'Current ESP-DL combines ESP-PPQ, the .espdl format, static memory planning, and on-device profiling in one toolchain for model conversion, loading, execution, and measurement.', source: {label: 'Official ESP-DL introduction', url: 'https://docs.espressif.com/projects/esp-dl/en/latest/introduction/readme.html'}},
      ],
      bridge: 'This history leaves a clear engineering conclusion: model architecture determines only how fast a system might run. The preprocessing contract, quantized export, kernel coverage, task scheduling, and board-level regression tests determine whether it truly becomes a product feature.',
    },
    visual: {
      title: 'How one sample becomes a device event',
      description: 'The player illuminates each data form and responsibility boundary, then returns field failures to the dataset at the end.',
      steps: [
        {icon: '🎙️', label: 'Collect and label', data: 'raw window + label + metadata', action: 'Record raw samples by device and scenario, then freeze labeling rules', insight: 'The source data defines the world the model can recognize'},
        {icon: '🧽', label: 'Equivalent preprocessing', data: 'tensor[shape, dtype, range]', action: 'Apply identical crop, scale, and quantization rules on PC and firmware', insight: 'The input contract matters more than API names'},
        {icon: '🧠', label: 'Train and export', data: 'checkpoint → graph → quantized model', action: 'Fix inference mode and compare outputs at every stage', insight: 'Every conversion is a potential semantic boundary'},
        {icon: '📟', label: 'Execute on the board', data: 'arena + kernels + latency', action: 'Load the model, schedule kernels, and record memory and stage timing', insight: 'Being loadable does not mean being stable in steady state'},
        {icon: '🚨', label: 'Decide the event', data: 'score → threshold → state', action: 'Apply hysteresis, debounce, and fallback before driving a physical action', insight: 'A model score is not a final control command'},
        {icon: '🔁', label: 'Return field evidence', data: 'false positive / false negative', action: 'Save false detections, misses, and raw context for the next data review', insight: 'The closed loop turns a demonstration into a maintainable system'},
      ],
      loop: 'After human review, field samples reenter “Collect and label,” while the old test set remains frozen to determine whether the new version actually improves.',
    },
    analogyDetail: {
      title: 'A miniature factory turning fresh fruit into juice',
      story: 'Fruit arriving from the orchard varies in size, ripeness, and dirt. The lab first defines acceptance samples and a washing recipe, then trains an “inspector.” The production line must cut and weigh fruit according to exactly the same recipe, and inspection results still pass through thresholds and review before the bottling valve actually opens.',
      illustration: [
        {icon: '🧺', label: 'Fruit crates and retained samples', mapsTo: 'Real-device collection, labeling rules, and train/validation/test splits'},
        {icon: '🚿', label: 'Washing and cutting recipe', mapsTo: 'The preprocessing contract: sampling, resize, normalization, quantized input, and more'},
        {icon: '🔬', label: 'Miniature inspector', mapsTo: 'The quantized model, device kernels, and tensor arena'},
        {icon: '🚦', label: 'Bottling release light', mapsTo: 'Thresholds, debounce, fallback, and business events'},
      ],
      boundary: 'The analogy explains a closed loop and shared contracts, but a model is not a fixed-rule inspection machine: it outputs probabilities or scores that depend on the data distribution. When a new field condition was absent from training data, a perfectly matched recipe still cannot guarantee correctness; drift monitoring, rejection, and a safety state machine remain necessary.',
    },
  },
  {
    n: 7,
    t: 'Memory, Bandwidth, MCU, and NPU',
    s: 'Progress from “it runs” to “it runs reliably”',
    goal: 'Explain edge performance using peak RAM, sustained bandwidth, and compute budgets instead of parameter count alone.',
    concept: ['Flash/RAM/PSRAM/cache', 'Peak live memory', 'Roofline intuition for compute', 'DMA, alignment, and zero-copy', 'NPU delegation and fallback'],
    analogy: 'Flash is a warehouse, RAM is a workbench, cache is a parts tray at your elbow, and bandwidth is the speed of the material handler. A small model does not imply a small peak workbench.',
    diagram: 'Flash weights ─mmap/copy─► RAM/PSRAM ─► cache ─► MAC unit\n                              ▲ DMA / zero-copy',
    code: 'peak = weights + activations + workspace + io_buffers\nif peak > available_ram:\n    reduce_resolution_or_batch()\n    enable_streaming_or_tiling()',
    lab: 'Given a table of model layers, estimate peak activation and workspace memory at every layer, then plot RAM and latency against several input resolutions.',
    questions: ['Why might adding compute fail to accelerate a bandwidth-bound model?', 'Can PSRAM completely replace internal SRAM?', 'Why do NPU delegate boundaries affect performance?'],
    next: 'Vision Models',
    lesson: [
      {title: 'Peak memory comes from overlapping lifetimes, not adding file sizes', body: 'For every tensor in the computation graph, list its producer, last consumer, size, and memory capabilities, then draw live intervals before calculating the peak. Weights can often remain in mapped Flash, while activations, accumulators, and kernel workspace coexist in the same layer. Camera double buffers, task stacks, and network packets must also enter the system peak. A planner can reuse regions with non-overlapping lifetimes, but dynamic shapes, branches, and fallback alter the plan, so validate static estimates against the runtime high-water mark.'},
      {title: 'Use arithmetic intensity to decide whether the MAC unit or memory is waiting', body: 'Roughly divide each operator’s MACs by bytes moved from slower levels to build intuition for “operations per byte.” Low-intensity paths are more likely bandwidth-bound. A higher clock or more NPU units helps directly only when compute is the limit; under a bandwidth limit, first fuse operators, reuse tiled data, prevent intermediates from materializing, and improve layouts. Theoretical bandwidth is a ceiling. Sequential access, cache misses, bus contention, and refresh protocols determine the sustained value, so benchmark a realistic access pattern.'},
      {title: 'Zero-copy is an ownership protocol, not a pointer cast', body: 'Having camera DMA write directly into model input can remove one copy, but only when address capabilities, alignment, stride, cache coherence, and lifetime satisfy both parties. The CPU must not read before DMA completion, and inference must release the buffer before DMA reuses it; cached systems also need synchronization in the correct direction. Use an explicit state machine such as FREE, FILLING, READY, and IN_USE, and log timeouts and dropped frames. “Zero-copy” without these constraints often becomes intermittent tearing or stale data.'},
      {title: 'Judge NPU acceleration by net benefit across the complete partition boundary', body: 'A delegate partitions subgraphs according to operator, shape, dtype, and layout support. If it claims only a few middle layers, CPU→NPU quantization, layout transforms, buffer copies, and synchronization can consume the time saved by kernels; unsupported nodes may also fall back silently. Inspect runtime partition logs, time the accelerated subgraph and its boundary conversions separately, and compare with a CPU-only baseline. Prefer a smaller network that fits wholly on the NPU over a large one producing fragmented partitions solely because of advertised TOPS.'},
      {title: 'Accept a resource plan with steady-state curves, not one fastest run', body: 'Warm up under fixed inputs, frequency, and compiler options, then record stage cycles, bus or cache counters, peak heap, and power. Vary resolution and sample rate progressively to plot latency and memory curves. Long-running tests should include competing Wi-Fi, camera, and OTA loads and observe thermal throttling and tail latency. If outputs change after optimization, test numerical equivalence first. Reserve budget for fragmentation, abnormal logging, and firmware upgrades instead of promising all idle lab memory to the model.'},
    ],
    pitfall: 'Treating “PSRAM has more capacity” as “PSRAM can replace internal SRAM” can make DMA unavailable, reduce bandwidth, or create cache-coherence problems—especially when camera and display streams run together.',
    references: [
      ['ESP-IDF Memory Types', 'https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-guides/memory-types.html'],
      ['LiteRT for Microcontrollers', 'https://ai.google.dev/edge/litert/microcontrollers/get_started'],
    ],
    quiz: [
      {prompt: 'What should an edge-inference peak RAM budget include at minimum?', options: ['Weights only', 'Weights, activations, workspace, and I/O buffers', 'Only the model name', 'Only the FreeRTOS tick'], answer: 1, explanation: 'The peak is determined by buffers live at the same time. Weights may reside in Flash, while activations and temporary workspace often determine whether execution is stable.'},
      {prompt: 'For the classic ESP32, what does ESP-IDF MALLOC_CAP_DMA most closely mean?', options: ['Prefer external PSRAM', 'Allocate memory suitable for hardware DMA, usually excluding external PSRAM', 'Force allocation in Flash', 'Disable cache'], answer: 1, explanation: 'The official documentation states that this capability is DMA-suitable and excludes external PSRAM. Always confirm exact capabilities in the documentation for the chosen SoC and peripheral.'},
      {prompt: 'Why might adding MAC units fail to speed up a bandwidth-bound model?', options: ['MAC units delete weights', 'The processor still waits for weights and activations to move from memory', 'The input shape shrinks automatically', 'Quantization fails automatically'], answer: 1, explanation: 'When bytes per second are the bottleneck, additional compute sits idle waiting. First reduce reads and writes, improve layouts, or fuse operators.'},
    ],
    readingMinutes: 18,
    keywords: [
      {term: 'Peak live memory', definition: 'The total size of all buffers that must be retained at the same instant.', espAnalogy: 'Like task stacks and DMA descriptors that coexist during a context switch.'},
      {term: 'Bandwidth', definition: 'The amount of data that can be moved per unit time.', espAnalogy: 'Like SPI/I2S bus throughput: fast compute still waits for its data.'},
      {term: 'PSRAM', definition: 'High-capacity RAM connected through an external interface.', espAnalogy: 'Like an expansion warehouse with more space but different access and DMA rules.'},
      {term: 'Delegate', definition: 'A backend mechanism that assigns supported subgraphs to a specialized accelerator.', espAnalogy: 'Like forwarding supported peripheral requests to a coprocessor.'},
    ],
    recap: 'The previous chapter proved a functional loop for a small model. This chapter shifts attention from accuracy to resource budgets: which buffers are live together, whether data movement is the bottleneck, and whether the NPU delegate is actually being used.',
    nextPreview: 'The next chapter places these budgets into a real-time camera pipeline and observes how DMA, frame buffers, color formats, and postprocessing jointly affect results.',
    history: {
      intro: 'The history of edge performance repeatedly shows that multiply–accumulate units are merely the stove in a kitchen. How data reaches the stove, where it waits, and whether it is moved repeatedly often determines throughput and energy more strongly. MCU/NPU engineering consequently evolved from counting FLOPs to planning compute and data flow together.',
      milestones: [
        {year: '1982', title: 'Systolic arrays let data flow among processing elements', body: 'H. T. Kung systematically described systolic architectures: regularly arranged processing elements reuse and pass data in rhythm. This provided an important foundation for the data-flow design of later matrix accelerators.', source: {label: 'Original Why Systolic Architectures paper', url: 'https://doi.org/10.1109/MC.1982.1653825'}},
        {year: '2009', title: 'Roofline plotted compute and bandwidth ceilings together', body: 'The Roofline model connected peak compute with sustainable memory bandwidth through arithmetic intensity, turning “optimize arithmetic or reduce movement?” into a measurable, discussable question.', source: {label: 'Original UC Berkeley Roofline report', url: 'https://www2.eecs.berkeley.edu/Pubs/TechRpts/2008/EECS-2008-134.pdf'}},
        {year: '2016', title: 'Eyeriss quantified the energy cost of moving data', body: 'Eyeriss introduced a row-stationary dataflow that reused weights, activations, and partial sums through local storage and inter-PE communication, emphasizing that moving less data is itself acceleration.', source: {label: 'Original Eyeriss paper page', url: 'https://research.nvidia.com/publication/2016-06_eyeriss-spatial-architecture-energy-efficient-dataflow-convolutional-neural'}},
        {year: '2018', title: 'MCU kernels optimized runtime and workspace together', body: 'CMSIS-NN showed how SIMD, fixed-point computation, and partial im2col improve Cortex-M throughput while reducing memory use, demonstrating that kernel selection changes the peak working set.', source: {label: 'Original CMSIS-NN paper', url: 'https://arxiv.org/abs/1801.06601'}},
        {year: '2020', title: 'MCUNet co-designed networks and inference engines', body: 'MCUNet used TinyNAS to search for networks under device constraints and TinyEngine to schedule memory over whole-graph lifetimes, advancing joint optimization of models, runtimes, and hardware.', source: {label: 'Original MCUNet paper', url: 'https://arxiv.org/abs/2007.10319'}},
        {year: 'Present', title: 'Memory capabilities and coherence became explicit APIs', body: 'ESP-IDF distinguishes properties such as DMA suitability and byte accessibility through a capability allocator. On chips with caches and DMA, synchronization and ownership must also be handled explicitly.', source: {label: 'Official ESP-IDF memory-allocation documentation', url: 'https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-reference/system/mem_alloc.html'}},
      ],
      bridge: 'Resource analysis today should answer four questions together: which buffers are live simultaneously, how many buses each byte crosses, who executes every subgraph, and whether the measured bottleneck changes with input and temperature. Reporting only parameter count or peak TOPS misses the most expensive path.',
    },
    visual: {
      title: 'The resource journey of one activation tensor',
      description: 'Each step shows the tensor’s storage level, the movement taking place, and the resource truly limiting throughput at that moment.',
      steps: [
        {icon: '🗄️', label: 'Fetch weights', data: 'Flash/PSRAM → cache', action: 'Read the weights needed for this iteration one tile at a time', insight: 'Adequate capacity can still be limited by bandwidth from a slow level'},
        {icon: '📥', label: 'Receive input', data: 'DMA buffer → SRAM', action: 'Confirm alignment and ownership, then synchronize cache', insight: 'Zero-copy begins as a coherence contract'},
        {icon: '🧩', label: 'Plan live regions', data: 'input + output + workspace', action: 'Reuse non-overlapping memory regions according to lifetimes', insight: 'The working set present at one instant determines the peak'},
        {icon: '⚙️', label: 'Execute kernel', data: 'tile × weights → partial sums', action: 'Reuse data in registers and local storage as much as possible', insight: 'One less move can be worth more than several extra MACs'},
        {icon: '🚧', label: 'Cross a backend boundary', data: 'CPU layout ↔ NPU layout', action: 'Convert dtype/layout and synchronize the subgraph', insight: 'A delegate boundary charges a visible toll'},
        {icon: '📈', label: 'Measure and rearrange', data: 'cycles + bytes + peak RAM', action: 'Use the measured bottleneck to choose fusion, tiling, or lower resolution', insight: 'Evidence, not TOPS, should select the optimization target'},
      ],
      loop: 'Measurements feed back into memory planning: reduce movement when bandwidth dominates, shrink the tile or input when the working set overflows, and replace a kernel only when compute is saturated.',
    },
    analogyDetail: {
      title: 'A small restaurant during the dinner rush',
      story: 'A well-stocked cold room does not mean the kitchen can prepare many tables at once. Counter space determines how many plates can be spread out, a narrow aisle limits how quickly servers move food, and a very fast specialty oven accepts only the right trays. Every change of plate and every trip in or out costs time.',
      illustration: [
        {icon: '🧊', label: 'External cold room', mapsTo: 'Weights and data with high capacity but higher access cost in Flash/PSRAM'},
        {icon: '🔪', label: 'Preparation counter', mapsTo: 'Inputs, outputs, and workspace simultaneously live in internal SRAM'},
        {icon: '🛎️', label: 'Serving aisle', mapsTo: 'The memory bus, DMA, cache lines, and sustained bandwidth'},
        {icon: '🔥', label: 'Specialty oven', mapsTo: 'An NPU delegate supporting only certain operators, layouts, and dtypes'},
      ],
      boundary: 'The restaurant analogy is useful for capacity, throughput, and repacking costs, but real storage levels are not mutually exclusive rooms: cache replaces entries automatically, and DMA and CPU may access the same physical memory concurrently. Whether flush, invalidate, or alignment is required must come from the chip manual and measurement.',
    },
  },
];
